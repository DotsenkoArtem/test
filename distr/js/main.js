"use strict";const date=new Date,currentDay=date.getDay(),requiredDay=1,curYear=date.getFullYear(),curMonth=date.getMonth(),curDate=date.getDate();let differ;differ=requiredDay>currentDay?requiredDay-currentDay:requiredDay-currentDay+7;const nextDate=new Date(date.getFullYear(),date.getMonth(),date.getDate()+differ);let shortYear=String(nextDate.getFullYear())[2]+String(nextDate.getFullYear())[3];function addZero(e){return 1<=e&&e<=9?"0"+e:e}time.innerHTML=addZero(nextDate.getDate())+"."+addZero(nextDate.getMonth()+1)+"."+shortYear,time.setAttribute("datetime",date.getFullYear()+"-"+addZero(nextDate.getMonth()+1)+"-"+addZero(nextDate.getDate()));